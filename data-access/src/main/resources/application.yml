server:
  port: 8080
  servlet:
    context-path: /data-access
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/centime
    username: root
    password: Root
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: data-access
  jpa:
    hibernate:
      ddl-auto: update
logging:
  config: classpath:customLog.xml